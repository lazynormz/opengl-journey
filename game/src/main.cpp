#include <engine.hpp>

int main(int argc, char *argv[])
{
    Engine::Engine engine; // Create an instance of the Engine class
    engine.Run();          // Run the game loop
    return 0;              // Return success
}